# Angular 11 CRM with bootstrap.

This project is the front-end for nodejs typescript Backend.

## Development server

Execute `npm i` to install all dependencies and then `ng serve` to run and test. Navigate to `http://localhost:4200/`.